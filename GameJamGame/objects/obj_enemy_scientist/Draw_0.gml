// Determine movement direction
//if (dx == 0 && dy == 0) {
//    sprite_index = spr_scientist_idle					// Standing still
//} else if (abs(dx) > abs(dy)) {
//    if (dx > 0) sprite_index = spr_scientist_right_walk	// Moving right
//    else sprite_index = spr_scientist_left_walk;			// Moving left
//} else {
//    if (dy > 0) sprite_index = spr_scientist_down_walk	// Moving down
//    else sprite_index = spr_scientist_up_walk;			// Moving up
//}

// Inherit the parent event
event_inherited();